## CALLARO

Premium modern light-theme AI agency website built with React, Tailwind CSS, Framer Motion, and Lucide React.

This workspace now contains a single frontend app at the project root (no backend). To run locally:

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
```
